# create-ggez

Starter scaffolder for Web Hammer projects.

## Usage

```bash
bunx create-ggez my-game
```

You can also choose a package manager explicitly:

```bash
bunx create-ggez my-game --package-manager npm
```

The generated starter currently targets the vanilla Three.js runtime workflow.